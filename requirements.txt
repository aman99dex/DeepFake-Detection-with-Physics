# PhysForensics — Core Requirements
# Python 3.10+ required
# GPU recommended but not required (CPU training works)

# ── Deep Learning ─────────────────────────────────────────────────────────────
# PyTorch: install the correct version for your platform at https://pytorch.org
# The line below installs CPU-only. For CUDA/MPS see INSTALL NOTES below.
torch>=2.1.0
torchvision>=0.16.0

# ── Core Scientific Stack ─────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
Pillow>=10.0.0
matplotlib>=3.7.0
tqdm>=4.66.0

# ── Neural Network Utilities ──────────────────────────────────────────────────
einops>=0.7.0          # Tensor rearrangement
kornia>=0.7.0          # Differentiable geometry / image ops
timm>=0.9.0            # Pretrained vision models (ViT, ResNet, EfficientNet)

# ── Face Detection ────────────────────────────────────────────────────────────
facenet-pytorch>=2.5.3 # MTCNN face detector + alignment

# ── HuggingFace (dataset download) ────────────────────────────────────────────
huggingface_hub>=0.20.0
datasets>=2.16.0

# ── Web Dashboard ─────────────────────────────────────────────────────────────
flask>=3.0.0

# ── Image I/O (optional but recommended) ─────────────────────────────────────
# opencv-python is large; use headless variant on servers
# opencv-python-headless>=4.8.0   # Uncomment if you need video support

# ── INSTALL NOTES ─────────────────────────────────────────────────────────────
# CUDA GPU (Linux/Windows):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Apple Silicon Mac (M1/M2/M3 — uses MPS backend):
#   pip install torch torchvision  # standard pip is fine, MPS is included
#
# CPU only (any platform):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
